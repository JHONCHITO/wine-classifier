MONGODB_URI=mongodb+srv://wine_user_db_user:winer123@cluster0.5eu31eu.mongodb.net/?appName=Cluster0

# Usa el modelo optimizado (el compatible que revisamos)
CRYPTO_MODEL_PATH=backend/models/optimized_mlp_classifier_model.joblib

# Scaler que ya tienes
CRYPTO_SCALER_PATH=backend/models/crypto_scaler.joblib

# Clave XOR para la clase 4 (la misma que usamos para cifrar)
XOR_KEY_HEX=4b6579
